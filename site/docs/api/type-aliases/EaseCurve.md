# Type Alias: EaseCurve

> **EaseCurve** = (`t`: `number`) => `number`

Custom easing function mapping progress t (0..1) to animated value.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `t` | `number` |

## Returns

`number`
