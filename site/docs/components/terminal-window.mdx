---
title: TerminalWindow
sidebar_position: 6
---

import { DemoFrame } from "@site/src/components/DemoFrame";

# TerminalWindow

The `TerminalWindow` component displays terminal command-line sessions with window controls and line selection.

<DemoFrame src="/stageroutine/previews/terminal-window.html" />

```typescript preview="terminal-window.html"
// hide-start
import { Stage } from "stageroutine";
const stage = new Stage();
// hide-end
import { TerminalWindow } from "stageroutine";

const terminal = TerminalWindow({
  x: "center",
  y: "center",
  width: 500,
  lines: [
    "$ command",
    "Output line 1",
    "Output line 2",
  ],
});
// hide-start
stage.scene("Terminal").with(terminal);
stage.pause();
stage.mount("#stage");
// hide-end
```

## Line Selection

Clicking or dragging terminal lines highlights selected rows and dims unselected lines.

You can also focus lines programmatically with `focusLines(start, end)`:

```typescript
// Focus line 1
terminal.focusLines(1);

// Clear focus
terminal.unfocus();
```

## API Reference

For configuration options, see the [`TerminalWindow`](../api/functions/TerminalWindow.md) reference.
